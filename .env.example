# Copy this file to .env and fill in your API key.
# The agent mode (python -m src.main --agent) requires this.
# The batch simulation (python -m src.main) works without it.
ANTHROPIC_API_KEY=your_api_key_here
